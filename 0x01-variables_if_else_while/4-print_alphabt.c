#include<stdio.h>
/**
*main-print letters except q and e
*
*Return:always return 0
**/
int main(void)
{
char ch = 'a';
while (ch != z)
{
if ((ch != 'q') && (ch != 'e'))
putchar (ch);
ch++;
}
putchar ('\n');
return (0);
}
